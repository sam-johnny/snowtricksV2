{% extends 'base.html.twig' %}

{% block title %}Modifier le lien{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Modifier le lien</h1>
        <div class="col-3 my-4">
            <iframe width="100%" height="96%" src="{{ link_media.url }}"
                    title="YouTube video player" frameborder="0"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
                    allowfullscreen></iframe>
            <div class="text-center">
                {{ include('admin/link_media/_delete_form.html.twig') }}
            </div>
        </div>
        <hr>
        {{ include('admin/link_media/_form.html.twig', {'button_label': 'Modifer'}) }}
        <hr>
    </div>
{% endblock %}