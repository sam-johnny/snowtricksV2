{% extends 'base.html.twig' %}

{% block title %}Modifier Image{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Modifier Image</h1>

        <div class="col-3 my-4">
            <img src="{{ vich_uploader_asset(image)|imagine_filter('my_thumb') }}" class="my-2 mx-auto img-fluid"
                 alt="{{ image.post.title }}"
                 height="auto">
            <div class="text-center">
                {{ include('admin/image/_delete_form.html.twig') }}
            </div>
        </div>

        <hr>

        {{ include('admin/image/_form.html.twig', {'button_label': 'Modifer'}) }}

        <hr>

    </div>
{% endblock %}
